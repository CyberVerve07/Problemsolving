package dailycode.july27;

public interface Predictable {
    // Predicate is a functional interface in Java (java.util.function) that represents a boolean-valued function of one argument.
    // It is used to test whether an object satisfies a condition or not.
}
