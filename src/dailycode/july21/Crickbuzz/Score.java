package dailycode.july21.Crickbuzz;

public interface Score {   //100% contract based

    //n Java, an interface is a blueprint of a class that defines a set of abstract
    // methods without a body and constants. It specifies what a class should do
    // , but not how it should do it.
    // Interfaces are key to achieving abstraction and multiple inheritance in Java.
    // In java Interface have no constructor, no object , Overload krwa leta ha override
    // only abstract method no composition method in

    //Abstract Methods :: ja ya


       public abstract void checkScore();
       public  abstract  void  showResult();
        public  abstract  void  browserTeam();
        public  abstract  void   iccRanking();
         public  abstract  void  schedule();

}
